const express = require("express");
const app = express();
const port = 3000;

// TODO: Define a GET route on path "/"
// The response should be "Hello, World!"
// Hint: Use app.get()

// TODO: Start the server and listen on the defined port
// Log "Server is running on http://localhost:3000" to the console

/*
Expected Output:
- When you run: node app.js
- Console: Server is running on http://localhost:3000
- Browser (http://localhost:3000): Hello, World!
*/
